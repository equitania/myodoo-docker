# Backup Credentials Configuration
# IMPORTANT: This file should have restricted permissions (chmod 600)
# and should NEVER be committed to version control

credentials:
  # Global encryption key for all backups
  global_key: "your-secure-password-here"  # Replace with your actual password

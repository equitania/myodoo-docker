# Legacy Cleanup List for ownERP Server Environment
# Version 1.0.0 | 28.01.2026
#
# This file contains paths relative to $HOME that should be removed
# during getScripts.py execution. Lines starting with # are comments.
# Empty lines are ignored.
#
# Format: One path per line, relative to home directory
# Use wildcards: *.bak, .zcompdump* etc.

# =============================================================================
# OLD SCRIPTS (replaced by YAML-based versions)
# =============================================================================
backup_manager.py
container2backup_zstd.py
update_docker_myodoo.py

# =============================================================================
# DEPRECATED CSV CONFIGURATIONS (replaced by YAML)
# =============================================================================
# NOT listed here any more, deliberately. These four files ARE the customer's
# configuration, and this list deletes what it names — on a fresh Fish install,
# which is exactly the run that lifts an old server onto the new stack. The
# configuration was therefore deleted by the very upgrade that needed to read
# it, and the first anyone knew was a readiness report saying
# "container2backup.yaml not found" with no CSV left to convert.
#
# ownerp_migrate.py owns them now: it converts them to YAML and MOVES the
# originals to $HOME/legacy-csv/<timestamp>/. One file, one owner — and this
# owner archives instead of deleting.
#
#   container2backup.csv
#   container2backup_path.csv
#   docker2update.csv
#   rsync_targets.csv

# =============================================================================
# ZSH/OH-MY-ZSH ARTIFACTS (Fish is now primary shell)
# =============================================================================
.oh-my-zsh/
.shell.pre-oh-my-zsh
.zshrc.bak
.zshrc.bak.ohmyzsh
.zcompdump
.zcompdump-*

# =============================================================================
# BASH ARTIFACTS (not used anymore)
# =============================================================================
.bash_history
.bashrc

# =============================================================================
# MISCELLANEOUS CLEANUP
# =============================================================================
.selected_editor
.wget-hsts
restore-zip.sh

# =============================================================================
# OLD LOG FILES (optional - uncomment if desired)
# =============================================================================
# nginx_set_conf.log
# getscripts.log

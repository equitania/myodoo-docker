# UV tool packages
# One tool per line, optionally pinned as "name==1.2.3". Unpinned tools are
# installed at the latest PyPI release and kept current on every run via
# "uv tool upgrade --all" plus a PyPI version check.
nginx-set-conf
odoodev

# PIP packages
pip
wheel
setuptools
distro-info
odoorpc-toolbox
zstd

# System packages
# python3-yaml + python3-dotenv: imported by the root-run scripts
# (server_hardening.py, nginx-cert-guard.py, container2backup.py). Installed via
# apt, NOT pip — modern Debian/Ubuntu mark system python3 externally-managed
# (PEP 668), so `pip install` as root fails.
python3-yaml
python3-dotenv
zoxide
fastfetch